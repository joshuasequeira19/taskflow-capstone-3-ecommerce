// Loaded before app.js. Point this at your deployed backend before opening index.html,
// or set window.SHOPFRONT_API_URL directly in a <script> tag in index.html.
window.SHOPFRONT_API_URL = window.SHOPFRONT_API_URL || "http://localhost:3000";
